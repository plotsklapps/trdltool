{
  "rules": {
    ".read": false,
    ".write": false,
    "$date": {
      "$code": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['createdAt', 'buttons'])"
      }
    }
  }
}
